var deck = Deck;
var heart = Suit("mongo", "sort");
var card = 2 of heart;

deck.shuffle();
deck.draw();
deck.addCard(card);
deck.removeCard(0);

var cardValue = card.value();
var cardSuit = card.suit();
var cardColor = card.color();
var cardName = card.name();
print(cardSuit);
print(cardColor);